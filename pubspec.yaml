name: app_commerce
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.1
  fan_carousel_image_slider: ^0.1.2
  provider: ^6.1.2
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/uhren/
    - assets/brille/
    - assets/brillem/
    - assets/uhrenm/
    
   

